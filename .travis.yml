language: ruby
rvm:
  - 2.3.0
# Handle git submodules yourself
git:
  submodules: false
sudo: required
before_install:
  # Use sed to replace the SSH URL with the public URL,
  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  # then initialize submodules
  - git submodule update --init --recursive
  - sudo apt-get update -qq
  - sudo apt-get install -qq build-essential libsdl2-dev libsdl2-tff-dev freeglut3-dev libfreeimage-dev libgl1-mesa-dev libopenal-dev libpango1.0-dev libsdl-ttf2.0-dev libsndfile-dev libxinerama-dev
  - gem install bundler -v 1.11.2
